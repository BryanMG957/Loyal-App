<%= form_for(@service_type) do |f| %>
  <% if @service_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_type.errors.count, "error") %> prohibited this service_type from being saved:</h2>

      <ul>
      <% @service_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :billing_display_name %><br>
    <%= f.text_field :billing_display_name %><br>
  </div> 
  <div class="field">
    <%= f.label :default_price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br>
    <%= f.collection_select(:company_id, @allowed_companies, :id, :name) %>
  </div>
  <br> 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
